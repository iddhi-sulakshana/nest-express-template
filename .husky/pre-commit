echo "🔍 Running Pre-commit checks..."

# Run formatter
pnpm run format

# Check for changes after formatting
if ! git diff --quiet; then
  echo "✨ Some files were changed by formatting:"
  git diff --name-only
  echo "💡 Please review and stage them before committing."
else
  echo "✅ No changes after formatting."
fi


